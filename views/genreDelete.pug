extends layout

block content
  h1 #{title}: #{genre.name}

  if genreBooks.length
    p #[strong Следующие книги должны быть удалены перед удалением данного жанра.]
    div(style='margin-left:20px;margin-top:20px')
      h4 Книги
      dl
      each book in genreBooks
        dt
          a(href=book.url) #{book.title}
        dd #{book.summary}
  else
    p Действительно ли вы хотите удалить данный жанр?
    form(method='POST' action='')
      div.form-group
        input#genreid.form-control(type='hidden', name='genreid', value=genre._id)
      button.btn.btn-primary(type='submit') Удалить
